#include <stdio.h>           // 9. finding the area of the circle ( not sure it is correct or not.)

int main(){
    int r;
    printf("enter the value of radius r:\n");
    scanf("%d", &r);

    printf("The ares of circle with radius %d is %f", r, 3.14*r*r);
     return 0;
}